{
	"$schema": "https://schemas.wp.org/trunk/block.json",
	"apiVersion": 3,
	"name": "iframed-demos/admin-scoped-css-broken",
	"version": "0.1.0",
	"title": "Admin-Scoped CSS (Broken)",
	"category": "widgets",
	"icon": "warning",
	"description": "Styles the block with a selector keyed on the admin body class .wp-admin. The canvas iframe body has no such class, so the styling silently vanishes when iframed.",
	"textdomain": "iframed-editor-demos",
	"editorScript": "file:./index.js",
	"editorStyle": "file:./index.css"
}
